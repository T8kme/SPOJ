#include<iostream>

using namespace std;

int main() {
	int length, number;
	cin >> length;

	while (length > 0) {

	cin >> number;

	int factorial = 1;
	for (int i = 1; i <= number; i++) {
		factorial = factorial * i;
	}

	cout << (factorial / 10) % 10 << " " << factorial % 10 << endl;

	length--;
	}
	system("PAUSE");
	return 0;
}
